const router = require('express').Router();
const { Course } = require('../models');

router.get ('/', async (req, res) => {
    //Render Index Templates and pass it a UserName value
    });
    

module.exports = router;